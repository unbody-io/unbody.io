---
import { ClientRouter } from "astro:transitions";
import Header from "../components/Header.astro";
import SEO from "../components/SEO.astro";
import type { Props as SEOProps } from "../components/SEO.astro";
import ShaderBackground from "../components/ShaderBackground.astro";
import "../styles/globals.css";

interface Props {
  title?: string;
  description?: string;
  seo?: Partial<SEOProps>;
}

const {
  title = "Unbody Lab",
  description =
    "Where we question, explore, experiment and build adaptive thinking tools.",
  seo,
} = Astro.props;
---

<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="color-scheme" content="light dark" />
    <meta
      name="theme-color"
      content="#ffffff"
      media="(prefers-color-scheme: light)"
    />
    <meta
      name="theme-color"
      content="#111111"
      media="(prefers-color-scheme: dark)"
    />
    <link
      rel="preload"
      href="/fonts/sligoil-400.woff2"
      as="font"
      type="font/woff2"
      crossorigin
    />
    <title>{title}</title>
    <meta name="description" content={description} />
    <SEO title={title} description={description} {...seo} />
    <script>
    import { initThemeController } from "../scripts/theme-controller";
    initThemeController();
    </script>
    <ClientRouter />
  </head>
  <body class="overflow-x-hidden">
    <!-- Fixed Ambient Background -->
    <div class="fixed inset-0 z-0 pointer-events-none">
      <ShaderBackground />
    </div>

    <!-- Top & Bottom Fade Masks -->
    <div class="fixed top-0 left-0 w-full z-40 overlay-mask-top pointer-events-none">
    </div>
    <div class="fixed bottom-0 left-0 w-full z-40 overlay-mask-bottom pointer-events-none">
    </div>

    <Header />

    <main class="min-h-screen relative flex flex-col items-center">
      <div class="relative z-10 w-full max-w-3xl px-5 pt-28 pb-32 md:pt-40 md:pb-48">
        <slot />
      </div>
    </main>

    <div
      data-type="nav"
      class="type-ui-md fixed bottom-6 left-1/2 -translate-x-1/2 md:left-auto md:translate-x-0 md:right-6 z-50 flex items-center gap-3"
    >
      <a href="https://github.com/unbody-io" class="underline">GitHub</a>
      <span class="opacity-30">·</span>
      <a href="mailto:hello@unbody.io" class="underline">hello@unbody.io</a>
    </div>
  </body>
</html>
